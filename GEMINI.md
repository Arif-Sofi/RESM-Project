preface shell commands with docker-compose exec app for any commands that utilizes docker.
After running a Test file, seed the database again.
Explain each command that you run. And if possible insert explaination to each line of code.
